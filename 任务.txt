1.  **用户上传** 后，由 **前端** 将文件发送到**后端**。
2.  **后端** 使用 **OCR/解析工具** 提取病历**文本**。
3.  **后端** 构造**提示词**，将文本和提示词一起发送给**LLM API**。
4.  **LLM** 处理后返回**建议**。
5.  **后端** 对建议进行**后处理**（格式化、添加免责声明）。
6.  **后端** 将最终结果返回给**前端**，**前端**展示给用户。


基于paddlepaddle进行开发，模型推理部分需要预留有本地推理以及调用服务器推理两个方案
